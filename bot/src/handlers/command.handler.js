export class CommandHandler {
  constructor(bot) {
    this.bot = bot
  }
  async handleUserStart(msg){
    try {
        
    } catch (error) {
        console.log('handle user start error', error)
    }
  }
}
